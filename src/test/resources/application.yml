spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: test
    bootstrap-servers: ${spring.embedded.kafka.brokers}

fint:
  kafka:
    topic:
      org-id: no.fintlabs
    default-replicas: 1
    application-id: test